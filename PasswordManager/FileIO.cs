
// Methods for reading and writing the entry files.
public static FileIO
{
    // Reads and returns the text from the specified entry file.
    public static string ReadEntries(string pathToEntryFile)
    {
    }

    // Saves the specified entries to a file.
    public static void WriteEntries(string entries)
    {
    }
}
